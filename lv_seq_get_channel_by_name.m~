function out_struct = lv_seq_get_channel_by_name(in_seq,channel_name)

found = false;
out_struct = [];

groups = {in_seq.primary_analog, in_seq.digital, in_seq.secondary_analog};

for a = 1:3
	for b = 1:(groups{a}.dims(2))
		
	end
	if found
		break;
	end
end
	